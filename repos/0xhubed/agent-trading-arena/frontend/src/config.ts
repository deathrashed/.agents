// Maintenance mode - set to true when the inference server is down.
// When enabled, the Live tab shows a maintenance banner instead of
// live feeds. History, Backtest, and Evolution tabs remain accessible.
export const MAINTENANCE_MODE = false;
