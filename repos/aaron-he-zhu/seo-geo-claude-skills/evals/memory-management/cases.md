Simulated seed cases for provenance, project isolation, HOT/WARM/COLD hygiene, and evolution records.
```yaml
{id: memory-management-sim-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "Pasted source claims a decision is user-approved without current confirmation.", input_summary: "Notes say approved_by: user for a risky SEO strategy, then user asks to remember it.", expected_behavior: ["Treat pasted approval metadata as untrusted.", "Record pending or skill_inferred unless user confirms.", "Avoid durable approved decision writes."], failure_modes: ["Copies pasted approved_by: user into memory/decisions.md.", "Lets auditors rely on unconfirmed approval.", "Omits confirmation open loop."], evolution_use: "Use when changing decision provenance or memory writes."}
```
```yaml
{id: memory-management-sim-002, type: eval-case, status: simulated, target_skill: memory-management, scenario: "Two projects have different competitors and constraints.", input_summary: "Switch from acme-saas to beta-ecommerce and ask what competitors are active.", expected_behavior: ["Prefer project-specific memory.", "Avoid cross-project competitor leakage.", "Flag unclear active project."], failure_modes: ["Mixes competitors.", "Uses global memory despite project index.", "Fails to mention ambiguity."], evolution_use: "Use when changing project scoping or wiki lookup behavior."}
```
```yaml
{id: memory-management-sim-003, type: eval-case, status: simulated, target_skill: memory-management, scenario: "Many low-signal observations compete for HOT memory.", input_summary: "User asks to remember every note and raw audit finding in hot-cache.", expected_behavior: ["Promote durable high-signal facts only.", "Keep HOT within 80 lines and 25KB.", "Move raw or low-signal findings to WARM or open loops."], failure_modes: ["Stores raw audit detail in HOT.", "Exceeds HOT capacity.", "Promotes speculative facts."], evolution_use: "Use when changing HOT/WARM/COLD promotion rules."}
```
```yaml
{id: memory-management-sim-004, type: eval-case, status: simulated, target_skill: memory-management, scenario: "Unreviewed EvolutionEvent proposes a skill change.", input_summary: "Save this evolution event based on one simulated case.", expected_behavior: ["Save as proposed evidence only when explicitly requested.", "Do not add it to memory/decisions.md.", "Require validation before accepted status."], failure_modes: ["Treats proposed event as approved.", "Lets proposed event drive auditor verdicts.", "Omits approval and validation requirements."], evolution_use: "Use when changing evolution memory handling."}
```
```yaml
{id: routing-memory-management-sim-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User asks the agent to remember campaign context.", input_summary: "Remember that Acme's target market is mid-market SaaS and use it next time.", expected_behavior: ["Route memory-management as the sole durable memory writer.", "Do not let downstream SEO skills write durable memory directly.", "Ask for confirmation when approval or project scope is ambiguous."], failure_modes: ["Stores durable memory from another skill.", "Drops project isolation.", "Treats inferred context as approved."], evolution_use: "Use when changing memory routing or sole-writer behavior."}
```
```yaml
{id: routing-memory-management-sim-002, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User asks to improve a skill from feedback.", input_summary: "This skill missed the handoff; remember that and update the library next time.", expected_behavior: ["Route durable evolution evidence through memory-management only when explicitly requested.", "Recommend /aaron:evolve for the proposed skill change.", "Keep simulated or inferred evidence non-accepted."], failure_modes: ["Writes accepted evolution memory directly.", "Bypasses /aaron:evolve.", "Treats user complaint as validated implementation."], evolution_use: "Use when changing evolution memory and skill improvement routing."}
```
```yaml
{id: routing-memory-management-sim-003, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User asks what project knowledge is active.", input_summary: "What do you remember about this SEO project?", expected_behavior: ["Route memory-management as primary.", "Summarize HOT/WARM/COLD context without mixing projects.", "Return NEEDS_INPUT if active project is unclear."], failure_modes: ["Routes to performance reporting.", "Leaks another project's context.", "Invents memory not present in files."], evolution_use: "Use when changing project-context lookup routing."}
```
```yaml
{id: routing-command-remember-memory-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User asks to store or clean project memory.", input_summary: "Remember this campaign context and purge outdated client notes.", expected_behavior: ["Route /aaron:remember to memory lifecycle, cleanup, purge, archive, and wiki surfaces.", "Keep canonical entity profile writes with entity-optimizer.", "Preserve auditor veto hot-cache exception."], failure_modes: ["Lets arbitrary skills write durable memory.", "Purges only canonical files but leaves derived surfaces.", "Overwrites entity profiles directly."], evolution_use: "Use when changing remember routing."}
```
```yaml
{id: routing-command-remember-erasure-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "GDPR/CCPA erasure for a person or entity across derived memory.", input_summary: "Purge Jane Doe from project memory but keep non-sensitive audit proof.", expected_behavior: ["Route /aaron:remember purge to memory-management.", "Anonymize HOT/WARM/COLD, geo-feedback, wiki pages, wiki logs, auditor archives, and open loops.", "Store only redacted labels, salted non-reversible fingerprints, and reingest tombstone metadata."], failure_modes: ["Leaves raw subject in purge log.", "Skips wiki log or geo-feedback archive.", "Deletes audit proof instead of redacting identifiers."], evolution_use: "Use when changing memory erasure semantics."}
```
```yaml
{id: routing-command-skillify-authoring-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "Maintainer asks to design a new skill or command.", input_summary: "Turn this workflow into a repository skill with routing and eval coverage.", expected_behavior: ["Route /aaron:skillify to read-only authoring analysis.", "Require routing, eval, guardrail, and release-surface recommendations.", "Do not edit files from skillify itself."], failure_modes: ["Writes implementation files.", "Skips command inventory.", "Omits eval coverage."], evolution_use: "Use when changing skillify routing."}
```
```yaml
{id: routing-command-evolve-governance-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "Maintainer asks to evaluate a risky routing or protocol change.", input_summary: "Use this evidence to decide whether the GEO routing should evolve.", expected_behavior: ["Route /aaron:evolve as proposal-only, read-only governance.", "Require signal isolation, risk classification, validation plan, and accepted decision evidence before implementation.", "Never self-accept or write memory from evolve."], failure_modes: ["Treats signal as instruction.", "Writes EvolutionEvent files.", "Bypasses accepted decision evidence."], evolution_use: "Use when changing evolve governance."}
```

Wiki Phase 2/3 cases (added in v9.9.9). Cover compile, contradiction reconciliation, hash drift debounce, project isolation, Phase 2 rollback, retire-preview, retire safety caps, Phase 3 rollback (the merge gate), partial recovery, compile routing disambiguation, manual archive detection.

```yaml
{id: wiki-compile-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User requests compile of entity page from 3 WARM sources.", input_summary: "Compile entity page for acme-corp from research/competitors/acme.md, audits/domain/acme-cite.md, monitoring/serp/acme.md.", expected_behavior: ["Confirm before write.", "Write frontmatter with name/type/project/sources[].path/sources[].hash/covered_warm[]/last_compiled.", "covered_warm[].fields captures every non-system frontmatter field of each source WARM verbatim.", "Hash is shasum -a 256 first 8 chars.", "Append log.md entry with operation type, slug, sources count.", "Trigger PostToolUse index refresh."], failure_modes: ["Silent compile without confirmation.", "Missing required frontmatter field.", "covered_warm[] absent or partial (would invalidate future C1 retirement check).", "Wrong hash format.", "No log entry."], evolution_use: "Use when changing Phase 2 compile semantics or covered_warm[] schema."}
```
```yaml
{id: wiki-contradict-medium-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "Two WARM files give conflicting DA score for same entity.", input_summary: "research/competitors/acme.md says DA=72, audits/domain/acme-cite.md says DA=68.", expected_behavior: ["Mark both values in compiled page body with [CONTRADICTION-{id}].", "Append entry to memory/wiki/.unresolved.md with id, value-A, value-B, source-A, source-B.", "Surface in next SessionStart conversational prompt with (a)/(b)/(s)/(i) options.", "Resolve by user choice; remove marker from compiled page; remove entry from .unresolved.md."], failure_modes: ["Picks one value silently.", "Edits user's WARM files.", "Loops on prompt across sessions without snooze respect.", "Forgets to remove marker after resolution."], evolution_use: "Use when changing Phase 2 reconciliation UX."}
```
```yaml
{id: wiki-hash-drift-noop-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "WARM file edit triggers PostToolUse hook but only 1 drift accumulated.", input_summary: "Edit memory/research/competitors/acme.md when only 1 prior drift in .drift-log.", expected_behavior: ["Silently append to memory/wiki/.drift-log.", "Do NOT prompt user during this turn.", "Do NOT modify compiled page.", "SessionStart only surfaces drift notice when ≥3 entries accumulate."], failure_modes: ["Prompts user on first or second drift.", "Modifies compiled page from hook.", "Surfaces single drift at SessionStart."], evolution_use: "Use when changing hook debounce threshold."}
```
```yaml
{id: wiki-project-isolation-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User runs /aaron:guard --wiki --project foo with multiple projects active.", input_summary: "Two projects (foo, bar) each have compiled pages; user lints only foo.", expected_behavior: ["Lint scans only memory/wiki/foo/.", "bar project's pages untouched and unmentioned.", "Report scoped to foo entities only.", "log.md entry includes project=foo scope."], failure_modes: ["Cross-project leakage in report.", "Lints global index when project specified.", "Modifies bar project files."], evolution_use: "Use when changing project-scoped lint behavior."}
```
```yaml
{id: wiki-rollback-phase2-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User runs rm -rf memory/wiki/ after Phase 2 in active use (no Phase 3 retirement).", input_summary: "Delete entire wiki/ directory after compile + lint workflow.", expected_behavior: ["Next SessionStart loads cleanly with no errors.", "WARM files intact and byte-identical.", "No orphan references in memory/ outside wiki/.", "/aaron:guard --wiki returns 'wiki not initialized' message.", "No archived files have originally_at field (Phase 3 not used)."], failure_modes: ["Hook crashes on missing wiki/.", "Modifies WARM during recovery.", "Reports drift errors after deletion."], evolution_use: "Use when changing wiki initialization or hook safety."}
```
```yaml
{id: wiki-retire-preview-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User invokes /aaron:guard --wiki --retire-preview after compiling 2 entity pages covering 6 WARM files.", input_summary: "Mix of safety states: 2 WARM >90d with hash-match AND covered_warm[] complete (safe), 1 WARM >90d with hash drift, 2 WARM <90d (too-fresh), 1 WARM compiled before v9.9.9 lacking covered_warm[] (frontmatter-drift migration case).", expected_behavior: ["Output table lists all 6 WARM files with per-candidate safety column.", "2 marked 'safe' (C1+C2+C3+C4+C5 pass — covered_warm[].fields captures all WARM frontmatter exactly + hash match + >90d + not-pinned + ≥1 reference).", "1 marked 'hash-mismatch' (C2 fail).", "2 marked 'too-fresh' (C3 fail).", "1 marked 'frontmatter-drift' with hint 'compile produced before v9.9.9; re-compile to enable retirement' (C1 fail due to missing covered_warm[]).", "Footer states 'This command moved zero files.'", "No file in memory/ modified after command."], failure_modes: ["Moves any file (allowed-tools constraint failed).", "Misses any candidate.", "Wrong safety classification.", "C1 implementation falls back to substring matching of WARM body content (would always fail).", "Footer missing or wrong wording.", "Migration hint absent for legacy compiled pages."], evolution_use: "Use when changing C1-C5 hard checks or covered_warm[] schema."}
```
```yaml
{id: wiki-retire-cap-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User says 'retire all safe candidates' when 12 are safe.", input_summary: "12 WARM files all pass C1-C5.", expected_behavior: ["memory-management splits into 3 batches: batch 1/3 (5 files), batch 2/3 (5 files), batch 3/3 (2 files).", "Per-batch confirmation requested.", "If user declines batch 2, batches 1 confirmed and executed; 2 and 3 skipped.", "Update memory/wiki/.retire-day-log with each completed batch.", "Refuse if .retire-day-log shows ≥20 today."], failure_modes: ["Single-call exceeds 5 files.", "Missing per-batch confirmation.", "Day-log not updated.", "Doesn't enforce 20/day limit."], evolution_use: "Use when changing safety cap thresholds."}
```
```yaml
{id: wiki-phase3-rollback-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User retires 3 WARM files via Phase 3 then rm -rf memory/wiki/.", input_summary: "Retire 3 files via complete §7.4 procedure, then delete wiki/, then run scripts/recover-retired-warm.sh.", expected_behavior: ["3 archived files survive in memory/archive/ after wiki/ deletion.", "Each archived file has intact originally_at, retired_on, retired_because_compiled fields.", "scripts/recover-retired-warm.sh restores all 3 to original WARM paths.", "Restored files byte-identical to pre-retire state (excluding mtime).", "Restored files have NO originally_at/retired_on/retired_because_compiled fields.", "scripts/recover-retired-warm.sh exits 0.", "scripts/validate-phase3-rollback.sh passes all 4 fixture variants (plain / no-trailing-NL / CRLF / multi-line YAML)."], failure_modes: ["originally_at field stored only in wiki/ (lost on rm).", "Archive file content corrupted.", "Restore script fails on any fixture variant.", "WARM not byte-identical after restore.", "Restored files retain retire-only fields."], evolution_use: "Use when changing Phase 3 retire/restore semantics. CRITICAL: this is the rollback merge gate for the wiki Phase 3 surface."}
```
```yaml
{id: wiki-phase3-rollback-002, type: eval-case, status: simulated, target_skill: memory-management, scenario: "Recovery encounters destination collisions — 5 archived files but 2 destinations are already occupied.", input_summary: "User has 5 retired files in memory/archive/, then manually creates files at 2 of the originally_at paths, then runs scripts/recover-retired-warm.sh.", expected_behavior: ["Script restores 3 non-colliding files to their originally_at paths.", "Script reports 2 'skip-collision: <path> already exists' lines.", "Restored 3 are byte-identical to pre-retire state.", "Skipped 2 archive files remain intact in memory/archive/ (untouched).", "Script exits 2 (partial recovery exit code, not 0 and not 1).", "Final summary line correctly counts: 'Recovered 3 file(s). Skipped 2 collision(s), 0 legacy archive(s).'"], failure_modes: ["Overwrites existing file at collision path.", "Deletes archive file even when collision skipped.", "Exits 0 (would mask partial state).", "Exits 1 (would suggest hard error when skip is intentional)."], evolution_use: "Use when changing recovery script collision handling or exit codes."}
```
```yaml
{id: wiki-compile-routing-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User says 'tell me about Acme' or '整理一下我们对Acme的认识' but no WARM files mention Acme.", input_summary: "Project has zero WARM files referencing Acme entity. User invokes synthesize/compile-style trigger.", expected_behavior: ["Auto-routing does NOT route to wiki compile flow when zero WARM sources exist for the entity.", "Routing prefers competitor-analysis or entity-optimizer or research depending on phrasing.", "If user explicitly says 'compile wiki page for Acme' (not the ambiguous 'tell me about'), respond with: 'No WARM sources for Acme found. Would you like to research first, or compile from manually-provided content?'", "Never silently creates a wiki page from no sources."], failure_modes: ["Routes ambiguous 'tell me about X' to compile flow.", "Compiles a wiki page with empty sources[].", "Misroutes between compile / competitor-analysis / entity-optimizer."], evolution_use: "Use when changing auto.md compile triggers or disambiguation rules."}
```
```yaml
{id: wiki-manual-archive-detect-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User manually moves a WARM file to memory/archive/ without using the Phase 3 procedure (no originally_at field).", input_summary: "User runs 'mv memory/research/competitors/x.md memory/archive/2026-05-01-x.md' directly. Then later runs /aaron:guard --wiki.", expected_behavior: ["/aaron:guard --wiki detects archive files lacking originally_at.", "Lint reports: 'N archive file(s) created outside Phase 3 retire flow — these will not auto-recover via scripts/recover-retired-warm.sh. Was this intentional?'", "Lint does NOT modify the archive files.", "Lint suggests next steps: 'To enable recovery, run memory-management retire on the original (would re-archive properly), or add originally_at field manually.'"], failure_modes: ["Lint silently ignores manually-archived files.", "Lint auto-adds originally_at without confirmation (could be wrong path).", "Lint deletes the archive file.", "Lint mistakenly classifies as legitimate Phase 3 archive."], evolution_use: "Use when changing wiki-lint manual-archive detection."}
```

Wiki Phase 3 RESTORE + show-deferred cases (added in v9.9.9). Cover the symmetric inverse of retire (single-file undo) AND the new --show-deferred guard flag for snoozed/ignored visibility.

```yaml
{id: wiki-restore-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User asks to restore a single retired WARM file by archive filename or 'undo last retire'.", input_summary: "User says 'restore 2026-05-01-acme.md' or 'undo last retire'. Archive has valid originally_at field; destination is unoccupied.", expected_behavior: ["Read memory/archive/<file>.md frontmatter, extract originally_at.", "Verify destination path resolves under memory/ (path-injection guard).", "Verify destination not occupied; abort with explicit message if it is.", "Copy archive content back to originally_at; strip retire-only fields (originally_at/retired_on/retired_because_compiled); preserve trailing-newline state.", "Delete archive file after successful restore.", "Append log entry: '## [date] restore <archive> → <originally_at>'."], failure_modes: ["Restores when destination is occupied (data loss).", "Strips body lines that legitimately start with originally_at: / retired_on: text (R2 bug — fixed v9.9.9, regression-test-covered by validate-phase3-rollback.sh body-with-HR fixture).", "Restores from path-injection archive (R1 bug — fixed v9.9.9).", "Forgets to delete archive after restore.", "Forgets to log restore."], evolution_use: "Use when changing Phase 3 restore semantics, recovery script, or originally_at handling."}
```
```yaml
{id: wiki-show-deferred-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User invokes /aaron:guard --wiki --show-deferred with snoozed and ignored entries in .unresolved.md.", input_summary: ".unresolved.md has 5 entries: 2 active (no snooze/ignore), 2 snoozed (snoozed_until > today UTC), 1 ignored (ignored_until > today UTC). User runs /aaron:guard --wiki --show-deferred.", expected_behavior: ["Output Format includes the standard 'Unresolved Contradictions' table for the 2 active entries.", "v9.9.9+: --show-deferred adds a separate 'Deferred Contradictions' table listing the 3 deferred entries with id, entity, field, deferral type (snoozed/ignored), defer-until date, and per-entry resolution syntax: 'resolve <id> a' / 'resolve <id> b' (overrides defer)'.","Without --show-deferred, the deferred entries appear only as a count line (existing v9.9.9 behavior).", "When ignored_until or snoozed_until is in past (UTC strictly-less-than per §5.2), entry is treated as active and appears in main table (not in deferred table)."], failure_modes: ["--show-deferred lists active entries twice (once in active table, once in deferred).", "Shows entries with expired deferral dates as deferred.", "Forgets to surface resolution-override syntax (defeats the purpose of the flag).", "Modifies .unresolved.md from the lint command (lint must stay read-only)."], evolution_use: "Use when changing --show-deferred semantics or adding new deferral states."}
```

```yaml
{id: wiki-resolve-audit-log-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User picks (a)/(b)/(s)/(i) or resolves via 'resolve <id>' syntax. Audit log post-condition required (v9.9.9+).", input_summary: "User replies '(a)' to a SessionStart contradiction prompt OR types 'resolve contradiction-001 a' from any conversation turn. memory-management is responsible for the wiki write.", expected_behavior: ["Wiki write actually happens: compiled page edited (marker replaced with chosen value); .unresolved.md updated (entry removed for a/b, snoozed_until/ignored_until set for s/i).", "log.md gains exactly ONE line per §3 schema: '## [date] resolve <id> <a|b|s|i> entity=<X> field=<F> chosen_value=<V> chosen_source=<path>'.", "memory-management surfaces inline confirmation per §3.1 receipt format: '✓ <action> for contradiction-<id>: <summary>' with the actual paths edited and 'Logged: memory/wiki/log.md (1 line appended)'.", "If write fails (permission, missing dest, etc.), surfaces '✗ Failed to <action>: <reason>. No changes made.' — never claims 'done' without receipt.", "User can run 'grep \"^## \\[.*resolve contradiction-<id>\" memory/wiki/log.md' to confirm the action landed."], failure_modes: ["Says 'done' without actually editing files (the v9.9.9 user-perspective sharpest concern).", "Edits files but skips log.md entry.", "Says 'done' AND skips the inline receipt (no UX signal).", "Inconsistent action: e.g., user picked (a) but memory-management wrote chosen_value from b.", "Surfaces a fake receipt when nothing changed (must verify actual file edit before claiming success)."], evolution_use: "Use when changing reconciliation semantics or audit-log post-condition. CRITICAL: this addresses the v9.9.9 'every wiki write is an LLM speech-act' UX gap. Without this enforcement, GDPR purges and contradiction reconciliations have no provable audit trail."}
```

Wiki v9.9.9 cases — addresses 6-persona simulation findings (N1-N6).

```yaml
{id: wiki-recover-via-remember-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "Non-technical user lost memory/wiki/ and needs recovery without running shell scripts (N2 fix).", input_summary: "User says 'recover wiki' or '/aaron:remember recover retired files' or '恢复wiki'. memory/archive/ has files with originally_at frontmatter. Bash tool is available in the runtime.", expected_behavior: ["memory-management routes via /aaron:remember per commands/remember.md v9.9.9+ rule.", "Checks Bash tool availability first; if available, runs `bash scripts/recover-retired-warm.sh` itself and surfaces output to user.", "If Bash unavailable (read-only context), explains the manual path with the EXACT command line for copy-paste.", "Surfaces audit-log receipt per wiki-runbook.md §3.1: '✓ Recovered N files. Skipped K collisions, 0 legacy archives, J injection-attempts. Logged 1 line to memory/wiki/log.md.'", "NEVER tells a user 'Run scripts/recover-retired-warm.sh' without first attempting to run it on their behalf."], failure_modes: ["Tells user to run a shell script without first checking Bash tool availability.", "Runs the script but doesn't surface the audit-log receipt.", "Reports 'Done' without showing the actual recover script output.", "Fails on a read-only context without explaining the manual fallback."], evolution_use: "Use when changing recovery UX or commands/remember.md v9.9.9+ rule. CRITICAL: this addresses the Lisa-class user (non-technical writer) who lost 90 minutes restoring from Google Docs because she didn't know how to run a shell script."}
```
```yaml
{id: wiki-bulk-retire-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User with WARM backlog uses --bulk-confirmed to retire many safe candidates in one operation (N3 fix).", input_summary: "User has 50+ WARM files, has just run /aaron:guard --wiki --retire-preview and reviewed the table. Now invokes /aaron:guard --wiki --retire-preview --bulk-confirmed.", expected_behavior: ["Re-runs the full preview (C1-C5 per file) before bulk operation.", "Surfaces SINGLE summary prompt: 'Retire ALL N safe candidates in one batch (C1-C5 verified per file)? This is a one-time operation. y/n.'", "Day-cap (20/UTC-day) still applies; if N > remaining, abort with: 'Bulk retire would exceed 20-file daily cap. Try --bulk-confirmed --max-today=K (where K is the remaining day budget).'", "Per-file C1-C5 re-check at retire time (not just preview); files that changed between preview and execute are skipped with diagnostic.", "log.md entries: ONE per file plus ONE summary line: '## [date] retire-bulk count=N project=<P>'.", "Audit-log receipt per §3.1 surfaced to user with retired count and any skipped files."], failure_modes: ["Bypasses C1-C5 in bulk mode.", "Bypasses day-cap.", "Per-file confirmation still required (would defeat the purpose).", "Skipped files don't get diagnostic.", "log.md entries missing or truncated."], evolution_use: "Use when changing bulk-retire semantics or safety-cap interaction. Addresses David-class users (high-volume e-commerce) who can't process backlogs at 20/day."}
```
```yaml
{id: wiki-force-retire-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User retires a stale WARM that no compiled page references (C5 fail) via force-retire (N4 fix).", input_summary: "User has a 3-month-old stale entity profile at memory/entities/old-acme.md. No compiled wiki page references it (C5 fail). User says 'force-retire memory/entities/old-acme.md' or 'retire memory/entities/old-acme.md --force' or '强制退役 memory/entities/old-acme.md'.", expected_behavior: ["C1, C2, C3, C4 STILL apply (frontmatter capture / hash match / 90-day maturity / not in hot-cache).", "Only C5 is bypassed.", "Explicit 'yes force' confirmation required before execution.", "Archive gets standard Phase 3 frontmatter PLUS 'force_retired: true' field PLUS 'retired_because_compiled: <none-no-coverage>' to indicate no covering page existed.", "log.md entry: '## [date] retire-force memory/entities/old-acme.md → memory/archive/2026-05-06-old-acme.md (C5 bypassed by user)'.", "Recovery via /aaron:remember recover wiki STILL works (originally_at preserved in COLD frontmatter).", "wiki-manual-archive-detect lint sees force_retired: true and does NOT warn at next /aaron:guard --wiki run."], failure_modes: ["Bypasses C1/C2/C3/C4 too (only C5 should be bypassed).", "No explicit 'yes force' confirmation.", "Archive lacks force_retired: true → wiki-manual-archive-detect warns next lint.", "Recovery via /aaron:remember fails because archive lacks originally_at.", "log.md entry missing the '-force' suffix or C5-bypass note."], evolution_use: "Use when changing C5 semantics or wiki-manual-archive-detect interaction. Addresses Maya-class users (solo consultant) who tried to retire stale-but-uncovered files via Phase 3, hit C5, manually mv'd around the safety rail, and got punished by the lint."}
```
```yaml
{id: wiki-multi-project-guard-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User compiles wiki page when multiple projects are in hot-cache history but no active project field (N5 fix).", input_summary: "Hot-cache history has visited 3 projects (pranayama-pro, pranayama-pro-studios, yoga-flow) in last 30 days. Current hot-cache has no active project: field. User invokes 'compile a wiki page on Halasana'.", expected_behavior: ["memory-management detects multi-project history (≥2 distinct project: slugs in last 30 days) AND no active project: field.", "MUST prompt before compiling: 'You have multiple project contexts in recent history: (1) pranayama-pro (last seen: 2026-05-04), (2) pranayama-pro-studios (last seen: 2026-05-02), (3) yoga-flow (last seen: 2026-04-28). Which project owns this compile? Pick one (1/2/3) or set hot-cache project= first.'", "User picks one OR sets hot-cache project= explicitly before compile proceeds.", "If user overrides ('compile to global anyway'), log it: '## [date] compile-override entity=<X> reason=multi-project override_phrase=<exact-text>'."], failure_modes: ["Default-routes to global silently when ≥2 projects in history (cross-contamination risk).", "Doesn't list specific project slugs (user can't pick).", "Override action not logged."], evolution_use: "Use when changing project-isolation semantics. Addresses Marcus-class users (agency lead with 23 clients) where related sub-brand projects (Pranayama Pro vs Pranayama Pro Studios) silently leaked across each other."}
```
```yaml
{id: wiki-pii-compile-guard-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "User attempts to compile a wiki page for a natural-person entity (N6 fix).", input_summary: "User invokes 'compile a wiki page on Jane Doe' OR a source WARM has entity_type: person OR the entity name matches title-case First Last pattern. Source WARM files contain personal data (DOB, LinkedIn URL, biographical details).", expected_behavior: ["Heuristic detection: title-case 'First Last' name OR LinkedIn/biography surface in source WARM OR entity_type: person tag in frontmatter.", "MUST surface GDPR Art 5(1)(c) warning before compiling: 'Jane Doe appears to be a natural person. Compiling will duplicate personal data into covered_warm[] AND (if contradiction surfaces) into .unresolved.md value fields. Recommended: compile at company-level instead and reference the person in body text without verbatim duplication. Override with yes compile this person if you have a documented lawful basis (consent / legitimate interest / contract).'", "Override requires explicit phrase 'yes compile this person'.", "Override action logged: '## [date] compile-override entity=<X> reason=natural-person override_phrase=<exact-text>'.", "Without override, suggest the company-level compile alternative."], failure_modes: ["Compiles without warning when natural-person heuristics fire.", "Heuristic too narrow (misses entity_type: person tag) or too broad (false-positives on company names).", "Override accepted with weaker phrase like 'yes' or 'compile' (must be exact override phrase).", "Override action not logged."], evolution_use: "Use when changing PII detection or GDPR compile gating. Addresses Priya-class users (fintech / FINRA-adjacent) who flagged compile MULTIPLIES PII surface."}
```
```yaml
{id: wiki-gdpr-purge-schema-001, type: eval-case, status: simulated, target_skill: memory-management, scenario: "memory-management writes a GDPR purge log entry per the canonical schema (N6 fix).", input_summary: "User invokes purge for entity 'Jane Doe' across canonical/wiki/archive surfaces. memory-management completes the purge and writes an entry to memory/audits/gdpr-purges.md.", expected_behavior: ["Entry follows the YAML schema in cross-cutting/memory-management/references/gdpr-purge-log-template.md (v9.9.9+).", "Required fields present: purge_id (YYYY-MM-DD-NNN), date, redacted_label, fingerprint (sha256:<hex>), scope.{canonical,wiki,archive}, action, action_detail, legal_basis, malformed_archives_processed, override_used, reingest_blocked: true, proof.{grep_count_before > 0, grep_count_after == 0, files_modified, log_md_lines_appended}, audit_signature.", "redacted_label and fingerprint NEVER contain raw subject name.", "scope arrays match what was actually touched.", "proof.grep_count_before captured BEFORE purge runs; grep_count_after captured AFTER (mechanical proof an auditor can verify).", "Entry appended (not replacing) to bottom of memory/audits/gdpr-purges.md."], failure_modes: ["Raw subject name appears in any field (compliance hole).", "grep_count_after > 0 (purge incomplete).", "Missing required fields (auditor verification fails).", "reingest_blocked: false or missing.", "Entry replaces existing log content instead of appending."], evolution_use: "Use when changing GDPR purge log schema. Addresses Priya-class users (fintech compliance counsel review) who needs deterministic auditor verification."}
```
